services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - api

  api:
    build: ./server
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3000"
